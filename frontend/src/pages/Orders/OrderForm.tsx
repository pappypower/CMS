import React from 'react';
import { Typography, Box } from '@mui/material';

const OrderForm: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Add/Edit Order</Typography>
      <Typography>Order form component - to be implemented</Typography>
    </Box>
  );
};

export default OrderForm; 